SES는 AWS에서 가장 단순한 서비스입니다

슬라이드 한 장 안에 다 담기는 내용이라 죄책감이 느껴질 정도네요, 너무 짧죠

어쨌든 요컨대 SES를 사용하면 이메일을 보낼 수 있습니다

SMTP 인터페이스를 사용할 수도 있고

AWS SDK의 사용도 가능합니다 이메일을 수신할 수도 있죠

S3, SNS, AWS Lambda 등과 통합할 수도 있고요

이렇게 이메일을 발신 혹은 수신하려면

IAM 권한이 필요할 텐데요

IAM은 SES와 완전히 통합될 수 있습니다

이게 다예요 정말 가장 간단한 서비스죠

알아야 할 내용은 이게 다지만 제가 이 내용을 말씀드린 이유는

가끔씩 시험 문제에서 무슨 이유로든지 간에

SES를 쓰게끔 함정을 파놓는 경우가 있기 때문입니다

SES 하면 이메일만 기억하세요 즉 시험 문제가 이메일에 관한 거라면

SES가 정답일 수 있지만 그 외에는 해당 사항 없습니다

이 강의는 여기까지고요 다음 강의에서 뵙겠습니다

이제 Amazon OpenSearch 서비스에 대해 알아보죠

Amazon OpenSearch는 여러분이 예전에 들어보셨을 수 있는 Amazon ElasticSearch의

후속작이에요

이름이 변경된 건 라이선싱 문제 때문이고요

그럼 비교해 보자면 DynamoDB에서

여러분은 인덱스나 기본키를 이용해서만 데이터베이스의 데이터를 쿼리할 수 있어요

하지만 OpenSearch를 쓰면, 이름에서 알 수 있듯이

설령 일부만 매칭되어도 모든 필드를 검색할 수 있어요

그래서 OpenSearch를 이용해서 앱에 검색 기능을 제공하는 경우가 아주 흔하죠

그리고 여러분은 다른 데이터베이스를 보충하는 용도로 OpenSearch를 사용할 거예요

그리고 OpenSearch는 검색에 사용할 수 있지만, 이름과는 달리

분석적 쿼리도 할 수 있어요

여러분은 두 가지 모드로 OpenSearch 클러스터를 프로비저닝할 수 있는데요

여러분은 관리형 클러스터 옵션을 쓸 수 있어요

그러면 실제로 물리적 인스턴스가 프로비저닝되어서 그걸 볼 수 있고요

또는 서버리스 경로를 택해서 서버리스 클러스터를 가질 수도 있어요

그러면 스케일링부터 운영까지 모든 걸 AWS에서 처리하죠

그리고 OpenSearch는 고유한 쿼리 언어가 있는데요, SQL을 네이티브하게 지원하지는 않지만

플러그인을 통해서 SQL 호환성을 활성화할 수 있어요

또 여러분은 다양한 곳에서 온 데이터를 받을 수 있는데요

예를 들어 Kinesis Data Firehose나 IoT, CloudWatch Logs나 어떤 커스텀 앱으로부터도 데이터를 받을 수 있어요

Cognito, IAM 통합을 통해 보안을 확보할 수 있고요

저장 중 암호화와 전송 중 암호화도 가능하죠

그리고 앞서 언급한 것처럼 OpenSearch 서비스 이외에 분석을 할 수도 있는데요

즉 여러분은 소위 OpenSearch 대시보드를 써서

OpenSearch 데이터 이외에 시각화 자료도 만들 수 있어요

그럼 OpenSearch를 사용하는 흔한 패턴을 소개해 드릴게요

DynamoDB가 있을 거고요, 거기 데이터가 들어 있을 거예요

사용자는 거기에 데이터를 삽입하고 삭제하고 업데이트하겠죠

그리고 여러분은 모든 스트림을 DynamoDB Stream에 전송해요

그걸 Lambda 함수가 잡을 거고요

그 Lambda 함수는 그 데이터를 실시간으로 Amazon OpenSearch에 삽입할 거예요

그리고 이런 프로세스를 거치면서 여러분의 앱은

특정한 항목을 검색할 능력을 갖게 되죠

예를 들어 항목 이름으로 부분 검색을 할 수 있고요

거기서 항목 ID를 찾을 수 있어요

그리고 항목 ID를 획득했으면

DynamoDB를 호출해서 실제로 DynamoDB 테이블로부터 전체 항목을 받게 되죠

이거 흔한 패턴이에요, 여기서 OpenSearch는 검색 능력을 제공하게 되죠

반면에 여러분의 기본 데이터 출처는 여전히 DynamoDB 테이블에 유지되는 거고요

CloudWatch Logs를 OpenSearch에서 받는 방법도 있는데요

그럼 먼저 CloudWatch Logs 구독 필터를 사용해서 데이터를

실시간으로 AWS에서 관리하는 Lambda 함수로 전송하고요

그러면 Lambda 함수는 실시간으로 모든 데이터를 Amazon OpenSearch로 전송하죠

또는 CloudWatch Logs를 쓰고 그 다음에 구독 필터를 사용하는 방식이 있는데요

이번에는 Kinesis Data Firehose가 구독 필터로부터 데이터를 읽을 수 있어요

그리고 이건 Data Firehose이기 때문에 근 실시간으로

데이터가 Amazon OpenSearch에 삽입될 거예요

Kinesis를 이용하는 다른 패턴을 보면, Kinesis Data Streams를 이용해서 데이터를 Amazon OpenSearch에 전송하는

방법은 두 가지가 있는데요

첫째는 Kinesis Data Firehose를 사용하는 방법이에요

역시 근 실시간 형태의 서비스고요

Lambda 함수를 이용해서 데이터 변환을 하고 그 다음에 데이터를

Amazon OpenSearch에 전송할 수도 있어요

또는 역시 Kinesis Data Streams를 사용하지만 이번에 여러분은 Lambda 함수를 만들 거고요

그게 실시간으로 데이터 스트림을 읽을 거예요

그리고 여러분은 커스텀 코드를 작성해서

Lambda 함수가 실시간으로 Amazon OpenSearch에 기록하게 할 거예요

이 패턴들은 모두 유효하고요

이제 여러분은 Amazon OpenSearch를 이용하는 거의 모든 아키텍처를 알았어요

이번 강의는 여기까지예요, 강의가 마음에 드셨길 바라고요, 저는 다음 강의에서 뵐게요

Amazon Atena에 대해 알아봅시다

Athena는 Amazon S3 버킷에 저장된 데이터의 분석을 도와주는 서버리스 쿼리 서비스입니다

데이터를 분석하려면 표준 SQL 언어를 사용해 파일을 쿼리해야 합니다, Athena의

아래 단은

Presto 엔진으로 구축되어 있고, 여기서 SQL 언어를 사용합니다

Athena의 목적은 사용자가 S3 버킷에 데이터를 로드하거나 여러분이 S3 버킷에 데이터를

로드했을 때 Athena 서비스를 사용해 데이터를 쿼리하여 분석하는 겁니다, Amazon S3 안에서

데이터를 옮기지 않고요

Athena는 서버리스로, S3 버킷에 있는 데이터를 그 자리에서 분석할 수 있습니다

지원하는 파일 포맷은 CSV, JSON, ORC, Avro, Parquet 등으로 다양하며

요금 정책도 매우 단순합니다

1TB의 데이터를 스캔할 때마다 고정된 비용을 지불하면 됩니다

서비스 전체가 서버리스기 때문에 따로 데이터베이스를 프로비저닝할 필요가 없습니다

Athena는 보통 Amazon QuickSight라는 도구와 함께 사용해 보고서 및 대시보드를 생성합니다

QuickSight는 나중에 자세히 살펴볼 거예요

어쨌든 Amazon QuickSight는 S3 버킷에 연결된 Athena에 연결해 사용합니다

Amazon Athena는 주로 임의 쿼리 실행, 비즈니스 인텔리전스, 분석, 보고 등에

사용하고, AWS 서비스에서 생성된 모든 종류의 로그를 쿼리하고 분석하는 데 사용합니다

VPC 흐름 로그나 로드 밸런서 로그, CloudTrail 추적 등을 분석할 수 있죠

시험과 관련해 팁을 드리자면, Amazon S3에서 서버리스 SQL 엔진을 사용해 데이터를 분석해야 할 경우

Athena가 답일 가능성이 높습니다

앞에서 성능 개선에 관해 말씀드린 적이 있는데, Athena의 성능 개선 방법도

시험에 출제되는 내용 중 한 가지입니다

먼저 1TB의 데이터를 스캔할 때마다 비용을 지불해야 하므로, 적절한 데이터 유형을 선택해

데이터 스캔이 적게 일어나도록 해야 합니다

이럴 때는 열 기반(Columnar) 데이터를 사용하면 비용을 아낄 수 있겠죠, 필요한 열만 스캔하면

되니까요

따라서 Amazon Athena에 적합한 데이터 포맷은 Apache Parquet과 ORC입니다

이 포맷을 사용하면 성능을 크게 개선할 수 있어요, 파일을 Apach Parquet 또는

ORC 포맷으로 변환하려면 또 다른 서비스를 사용해야 합니다

Glue 같은 서비스를요, Glue를 사용하면 데이터를 손쉽게 ETL 작업으로 변환할 수 있습니다, 예를 들면 CSV를

Parquet로 변환할 수 있죠

또한 스캔하는 데이터의 양을 줄이기 위해 데이터를 압축해 반환 용량의 크기를 줄여야 합니다

데이터를 압축하는 데 사용할 수 있는 메커니즘은

화면에 있는 목록을 참고해 주세요, 다음으로 특정 열에 계속해서 쿼리를 보내야 하는 상황이라면

데이터 세트를 분할할 수 있습니다, 데이터 세트를 분할하려면 S3 버킷에서

전체 경로 뒤에 슬래시(/)를 붙이고, 각 슬래시에 분할할 열 이름과 값을 넣으면 됩니다

Amazon S3에서 이런 식으로 데이터를 분할하여 구성하면, 데이터를 쿼리할 때

정확히 S3의 어느 경로에 있는 어느 폴더에서 데이터를 스캔해야 하는지 알 수 있습니다

여기 데이터 분할 예시가 있습니다

비행(flight) 데이터가 Parquet 포맷으로 저장돼 있어요, 여기에 '/year=1991'을 붙였습니다

year로 분할했으니까 연도별로 폴더가 하나씩 생길 겁니다

각 연도에는 월별 폴더가 있을 거고요

month는 1이고 월별 폴더 안에는 일별 폴더가 있을 겁니다, day는 1이고요

Athena에서 쿼리를 실행할 때 특정 연도, 특정 월, 특정 일로 필터링하면

Amazon S3의 어느 폴더에서 데이터를 가져와야 하는지 정확히 알 수 있기 때문에

해당하는 데이터만 가져올 수 있습니다

데이터를 아주 효과적으로 분할한 거죠

마지막으로 적용할 수 있는 성능 개선 방법은 큰 파일을 사용해 오버헤드를 최소화하는 겁니다

Athena는 Amazon S3에 용량이 작은 파일이 여러 개 있을 때보다

128MB 이상의 큰 파일이 있을 때 성능이 더 좋습니다, 사이즈가 큰 파일이 스캔하기도 쉽고 반환하기도

쉽기 때문입니다

Amazon Athena의 또 다른 기능은 연합 쿼리입니다

사실 Athena는 S3에 있는 데이터뿐만 아니라 모든 곳에서 데이터를 쿼리할 수 있습니다

관계형 또는 비관계형 DB에 있는 데이터와 AWS 또는 온프레미스에 있는 객체 및

사용자 정의 데이터 소스도 가져올 수 있습니다

방법이 뭘까요? 바로 '데이터 소스 커넥터'를 사용하는 겁니다

Lambda 함수를 사용해 Lambda 함수가 다른 서비스에 연합 쿼리를 실행하도록 만드는 겁니다

CloudWatch Logs, DynamoDB, RDS 등에 쿼리를 실행하도록요

아주 강력한 기능이에요

예를 들어 Athena가 있고 Lambda 함수가 있다고 해봅시다, 데이터 소스 커넥터마다 하나의

Lambda 함수가 필요합니다

여기서 Amazon Athena를 통해 ElastiCache, DocumentDB, DynamoDB, Redshift

Aurora, SQL 서버, MySQL, EMR 서비스 기반 HBase, 온프레미스 데이터베이스에 대한 쿼리를 실행하는 겁니다

물론 Amazon S3에도 실행할 수 있고요, 또한 조인이나 계산 등의 작업도 실행할 수

있습니다

그래서 이름이 '연합 쿼리'인 거죠

실행한 쿼리의 결과는 추후 분석을 위해 Amazon S3 버킷에 저장됩니다

지금까지 Amazon Athena를

살펴봤습니다

보신 것처럼 아주 유용한 서비스예요

도움이 됐길 바랍니다, 그럼 다음 시간에 뵙겠습니다

그럼 이제 Athena 서비스에서 SQL 쿼리 언어를 사용해 Amazon S3에 있는 데이터를 쿼리해 봅시다

Athena 서비스 메뉴로 들어왔어요

보시다시피 새로운 편집기 화면입니다

첫 번째 쿼리를 실행하기 전에 쿼리 결과 위치를 Amazon S3로 설정해야 해요

보기 설정(View Settings)을 클릭하고 관리 메뉴로 가보면

쿼리 결과 위치를 지정하는 메뉴가 있습니다

저는 먼저 Amazon S3의 버킷 생성 메뉴로 가서 버킷을 만들 거예요, 이름은

'stephane-

demo-athena-

eu-central-1'으로 할게요

이름은 고유해야 합니다

버킷을 생성해 보겠습니다

버킷이 생성됐어요

상세 보기를 클릭하면

제가 만든 버킷이 나옵니다, 여기서 버킷 이름을 복사한 다음

관리 메뉴로 돌아와서

's3://'를 입력하고 버킷 이름을 붙이겠습니다

저장을 클릭하면

쿼리 결과 위치가 이 S3 버킷으로 설정됩니다

저장 위치를 설정한 다음 편집기 화면을 새로고침하면, 여기에 이렇게

'AWSDataCatalog'라는 데이터 소스와 데이터베이스가 나오는 걸 볼 수 있습니다

보이시죠?

하지만 저는 작업에 사용할 데이터베이스를 새로 만들 겁니다

이 데이터가 없는 분들도 계실 테니까요

만드는 건 아주 간단합니다

우리는 Amazon S3에 있는 S3 액세스 로그를 쿼리할 거예요, 저는 'demo-s3-access-logs-stephane-

2020'이라는 버킷을 만들었습니다, 액세스 로그는 s3-logs 폴더에 저장돼 있어요

제가 하려는 건 데이터베이스를 생성하고 테이블을 만들어 여기 있는 객체가

나오도록 하는 거예요

어떻게 하면 될까요?

코드로 돌아가서 athena-s3-access-logs.sql 파일의 첫 줄에 있는 코드를 사용해 데이터베이스를

생성할 겁니다

데이터베이스를 생성하는 코드를 붙여 넣고 실행 버튼을 클릭해 볼게요

데이터베이스가 성공적으로 생성되었습니다

이제 데이터베이스로 's3_access_logs_db'를 지정하면

됩니다

이제 버킷 로그를 저장할 수 있는 외부 테이블을 생성해야 합니다

이 쿼리를 복사해 붙여 넣겠습니다

쿼리를 어디서 가져오면 되는지 알려드릴게요

인터넷에서 'athena s3 access logs'를 검색하면 이런 사이트가 나올 겁니다, 이 사이트에서

적절한 액세스 로그를 생성하는 데 필요한 쿼리를 얻을 수 있습니다

LOCATION에 있는 값은 바꿔줘야 합니다, 여기에 대상 버킷 이름과 접두어를

넣어야 합니다

s3-logs 폴더의 속성 창으로 가서 여기 있는

S3 URI를 복사해 버킷 이름과 접두어로 사용하겠습니다

복사한 주소를 여기에 붙여 넣을게요, 쿼리가 완성됐습니다

이 상태에서 실행하면

쿼리가 성공적으로 실행됩니다

이제 데이터베이스 밑에 'mybucket-logs'라는 테이블이 생겼습니다, 열이 아주 많은 테이블이죠

테이블에서 '테이블 미리보기'를 클릭하면 'SELECT *' 쿼리로 데이터를 가져오고 있습니다, 최대

10줄까지요

그리고 아래쪽에서 반환된 10줄의 데이터를 볼 수 있습니다

보이시죠?

이렇게 액세스 로그 데이터를 10줄 가져왔어요

아주 간단하죠

이제 모든 액세스 로그 데이터를 이 테이블에서 볼 수 있습니다, 정말 편리해요

이건 아주 기초적인 거예요, 이제 Athena를 사용해 좀 더 복잡한 쿼리를 실행해

보겠습니다

지금 보시다시피 우리가 따로 작업한 게 없어요

따로 데이터베이스를 구성하지도 않았고

프로비저닝하지도 않았습니다

모두 서버리스예요

모든 쿼리가 Athena 서비스에 의해 S3 버킷 내에 있는 데이터에 바로 실행됩니다

정말 강력한 기능이에요

쿼리 내용을 보면 테이블에서 'requesturi_operation', 'httpstatus'를 가져오고, 개수 파악을

위해 'count(*)'를 호출한 다음 GROUP BY로 묶습니다

좀 더 고급 SQL 쿼리죠, 이 쿼리를 실행하면 다양한 상태 코드가 결과로 나올 겁니다

발생 횟수도 함께 나올 거고요

예를 들어 GET 동작에서 404 상태는 49번 발생했습니다, HEAD의 200 상태는 81번 발생했고요, 이런 식이에요

GET의 403 상태는 14번 발생했고요

이런 식으로 데이터를 추출하면 버킷에서 어떤 요청 또는 상태가 발생하지 않았는지 시각적으로

확인할 수 있습니다, 또는 어떤 게 실패했는지 알 수 있죠

아주 유용한 기능입니다

마지막으로 버킷에서 403 오류가 발생한 경우를 살펴보겠습니다

누군가 권한이 없는 작업을 시도한 겁니다

악의적일 가능성이 높죠

누군가 권한 없는 작업을 시도했으니

찾아내 이유를 파악해야 합니다, 또는 권한 없는 사람이 버킷에 접근하려고 한 거라면 그것도

자세히 조사해 봐야겠죠

이 쿼리를 실행하면 44줄의 결과 데이터가 반환되는데 여기는 나오지 않습니다, 표시할

데이터가 너무 많거든요

총 44줄이 반환됐어요

각 줄의 데이터를 보면 어떤 쿼리가 생성되어 403 상태가 발생했는지 알 수 있습니다, 아주 유용하죠

다시 말씀드리지만 Athena의 강력함은 말 그대로 Amazon S3의 데이터를 쿼리할 수 있다는 겁니다

서버를 구성하거나 데이터를 변형하지 않고도 바로 복잡한 쿼리를 실행할 수 있죠

올바른 데이터 형식을 구성하고 데이터를 적절히 구분하기만 하면, 나머지는 Athena가 전부

알아서 처리합니다

좋아요

마지막으로 Athena에서는 최근에 사용한 쿼리와 저장된 쿼리를 볼 수 있습니다, 그리고 설정에서

쿼리 결과 암호화 여부와 대상 버킷을 변경할 수 있습니다

이번 강의는 여기까지입니다

도움이 됐길 바라요, 그럼 다음 시간에 뵙겠습니다



또 다른 분석 서비스로 Apache Kafka용 Amazon 관리형 스트리밍인 'Amazon MSK'를 살펴보도록

하겠습니다

그런데 Kafka가 뭘까요?

Kafka는 Amazon Kinesis와 같은 기능을 합니다

Kafka와 Kinesis 모두 데이터를 스트리밍할 수 있죠

MSK를 사용하면 AWS에서 완전관리형 Kafka 클러스터를 구성할 수 있고, 클러스터를 그 자리에서 생성하고

업데이트하고 삭제할 수 있죠

MSK는 클러스터에 Kafka 브로커 노드와 ZooKeeper 브로커 노드를 생성해

관리하고 우리는 해당 MSK 클러스터를 VPC와 여러 AZ에 배포할 수 있습니다, 최대 세 곳에 배포할 수 있죠

일반적인 Kafka 실패로부터 자동으로 복구할 수 있으며, 데이터는 원하는 기간만큼

EBS 볼륨에 저장할 수 있습니다

제 경험상 Apache Kafka를 구성하는 건 굉장히 복잡했습니다, 클릭 한 번으로

AWS에 Kafka를 배포할 수 있다는 건 아주 큰 장점이에요, Amazon MSK는 이런 서비스입니다

추가로 MSK를 서버리스로 쓸 수 있는 옵션이 있습니다, MSK에서 Apache Kafka를 실행하는 건

동일한데, 여기서는 여러분이 서버를 프로비저닝하거나 용량을 관리하는 게 아니라

MSK가 알아서 리소스를 프로비저닝하고 컴퓨팅 환경과 스토리지를 조절합니다

그럼 Apache Kafka의 역할은 뭘까요?

Apache Kafka는 데이터를 스트리밍하는 데 사용합니다, Kafka 클러스터에 브로커가 여러 개 있을 거고

우리가 만든 프로듀서에서 데이터를 생산할 겁니다

처리할 데이터는 Kinesis, Iot, RDS 등등에서 들어올 거고요

프로듀서는 데이터를 곧장 Kafka 토픽으로 전송할 겁니다, 해당 데이터는 복제되어 다른 브로커에

저장될 거고요

이제 Kafka 토픽으로 실시간 데이터 스트리밍이 들어옵니다, 그럼 컨슈머가 토픽을 폴링해

데이터 자체를 가져오겠죠

그럼 컨슈머에서 원하는 동작을 수행하면 됩니다, 데이터를 가공하거나 원하는 목적지로 보내는 거죠

EMR, S3, SageMaker, Kinesis, RDS 등등으로요

Kafka가 하는 일은 Kinesis와 비슷합니다, 하지만 이 둘에는 분명한 차이점이 있어요

그럼 Kinesis Data Streams와 Amazon MSK의 차이점을 알아봅시다

Kinesis Data Streams은 메시지 크기가 1MB로 제한되어 있습니다, Amazon MSK의 기본

설정값과 같죠, 하지만 MSK에서는 메시지 크기를 키울 수 있습니다, 10MB로 늘릴 수도 있죠

Kinesis Data Streams에서는 샤드라는 개념을 사용하고, MSK에서는 파티션과 Kafka 토픽이라는 개념을

사용하는데 서로 비슷한 개념이죠, Kinesis Data stream은 규모를 조정할 때

샤드 분할과 샤드 병합을 사용해 규모를 조정하는데

Amazon MSK에서는 토픽 규모를 조정할 수 있는 방법이 파티션 추가뿐이고 파티션 삭제는 불가능합니다

Kinesis Data Streams는 전송 중 암호화를 사용하고, MSK는 플레인텍스트 또는 전송 중 암호화를

사용할 수 있습니다

저장 데이터 암호화에는 모두 KMS를 사용하고요, 여기까지가 시험 범위에 해당합니다

추가로 몇 가지 차이점을 말씀드리면

Amazon MSK에서는 데이터를 원하는 기간만큼 저장해둘 수 있습니다

1년 이상 저장할 수도 있죠, EBS 스토리지 비용만 계속해서 지불한다면 상관없습니다

MSK에 데이터를 추가하려면 Kafka 프로듀서를 만들어야 합니다, MSK에서 데이터를 가져오는 건 여러 가지로

할 수 있고요

첫째는 Apache Flink용 Kinesis Data Analytics를 사용하는 겁니다

Flink 애플리케이션을 실행해 MSK 클러스터에서 직접 데이터를 읽을 수 있습니다

Glue를 사용해 ETL 작업을 스트리밍할 수도 있습니다, 이때는 Apache Spark Streaming을 사용하고요

Lambda 함수를 구성해 Amazon MSK를 이벤트 소스로 사용해도 되고요, 또는 직접

Kafka 컨슈머를 작성해 원하는 플랫폼에서 실행시켜도 됩니다

Amazon EC2 인스턴스나 ECS 클러스터, EKS 클러스터 같은 곳에서 실행시키는 거죠

이렇게 해서 Amazon MSK와 관련해 시험에 출제될 만한 내용을 모두 살펴봤습니다

도움이 됐길 바랍니다, 그럼 다음 시간에 뵙겠습니다
이번엔 AWS Certificate Manager 즉 ACM에 대해서 알아 봅시다

이 서비스를 통해 SSL/TLS 인증서를 쉽게

프로비저닝, 관리 및 배포할 수 있죠

그럼 이 인증서는 어디에 사용될까요? 이것은 HTTPS 엔드 포인트를 제공해

웹사이트에서 전송 중 암호화를 할 때 사용됩니다

예시를 살펴보죠 애플리케이션 로드 밸런서가 있고

이는 백엔드에서 HTTP를 통해 EC2 인스턴스를 가진

오토 스케일링 그룹과 연결되어 있습니다 하지만 우리는 최종 사용자에게

애플리케이션 로드 밸런서로의 HTTPS가 노출되기를 원하죠

이런 경우에 ACM을 사용하는데요

ACM이 도메인으로 연결되게 되면

이러한 TLS 인증서의 프로비저닝과

유지 관리를 허용해 줍니다

그러면 이들이 애플리케이션 로드 밸런서로 로딩되고

애플리케이션 로드 밸런서가 자동적으로

클라이언트에게 HTTPS를 엔드 포인트로 제공하게 되어

공용 웹을 거쳐 전송 중 암호화가 가능하게 됩니다

ACM은 공용 및 사설 TLS 인증서를 모두 지원하고

공용 TLS 인증서에 대한 사용은 무료입니다

TLS 인증서 자동 갱신이라는 아주 편리한 기능도 갖추고 있어

굉장히 유용하죠 저도 늘 사용하는 기능입니다

그리고 통합이 가능해서 TLS 인증서를 다른 서비스에서도 로딩해 줍니다

일래스틱 로드 밸런서나 CloudFront Distributions

API Gateway의 API 등이 모두 가능하죠

그러니 전송 중 암호화와 이런 인증서 생성을 돕는 것이

무슨 서비스인지 묻는 문제를 보면 ACM을 떠올리시면 됩니다

여기까지 하죠 다음 강의에서 뵙겠습니다

이제 인증서 관리자(Certificate Manager)를 사용해 봅시다

Certificate Manager를 입력하고 서비스를 열겠습니다

여기서 인증서를 요청할 수 있습니다

공개 인증서를 요청할 것입니다

다음을 클릭하고 이제 완전한 도메인 이름을 입력해야 합니다

acm-demo.stefantheteacher.com이 될 것입니다

이는 제가 현재 stefantheteacher.com을 소유하고 있기 때문입니다

그리고 이 도메인은 제 AWS 계정에 등록되어 있습니다

이것은 Route53에서 관리됩니다

그래서 검증 방법으로 DNS 검증을 사용하겠습니다

왜냐하면 이것이 매우 쉽기 때문입니다

ACM과 Route53 사이에는 직접 통합이 있습니다

키 알고리즘은 기본값으로 두고 요청(Request)을 클릭합니다

지금 요청은 DNS 검증인 검증을 기다리고 있습니다

그래서 위로 스크롤하면 도메인에서 검증 대기 중인 것을 볼 수 있습니다

그리고 이 값과 이 이름에 대한 레코드를 생성해야 합니다

그래서 여기를 클릭하고 Amazon Route 53에서 레코드 생성을 클릭할 수 있습니다

레코드 생성(Create records)을 클릭해 봅시다

이제 Amazon Route 53에 이 레코드들이 생성되었으니

검증에는 약간의 시간이 걸리겠지만 진행될 것입니다

AWS가 제가 이 도메인을 여전히 소유하고 있고, 레코드가 Route 53에 삽입되었는지 확인할 수 있을 때까지

영상을 잠시 멈추겠습니다

이제 제 인증서가 발급되었습니다

성공이니 이제 이것을 사용해야 합니다

그러면 Elastic Beanstalk으로 이동하겠습니다

이 방법은 환경을 시작하고 SSL 인증서를 로드하는 매우 쉬운 방법입니다

애플리케이션 생성(Create application)을 클릭하겠습니다

웹 서버 환경이 될 것이고, 이를 DemoTLSCertificate라고 부르겠습니다

다음으로 아래로 스크롤하여 관리형 플랫폼을 선택하겠습니다

Node.js가 될 것입니다

모든 설정은 기본값으로 두겠습니다

샘플 애플리케이션은 그대로 두겠습니다

하지만 여기서는 고가용성을 선택할 것입니다

그리고 실제로 사용자 지정 구성으로 설정하겠습니다

다음(Next)을 클릭합니다

무작위 EC2 키 페어를 선택하고 다음을 클릭합니다

여기서는 아무것도 건드리지 않습니다

데이터베이스에 대해서는 다음을 클릭합니다

루트 볼륨은 아무것도 건드리지 않습니다

보안 그룹도 건드리지 않습니다

지금은 로드 밸런서를 아래로 스크롤합니다

아무것도 건드리지 않습니다

잘 진행되고 있습니다

로드 밸런서 유형은 애플리케이션 로드 밸런서가 될 것입니다

여기서 중요한 부분입니다

전용 로드 밸런서를 사용할 것입니다

그리고 리스너를 추가하겠습니다

포트는 443이 될 것입니다

프로토콜은 Https가 될 것입니다

이제 SSL 인증서를 선택해야 합니다

이것이 방금 우리가 생성한 인증서입니다

다음으로 TLS에 대한 정책을 선택해야 합니다

여기서 원하는 정책을 선택할 수 있습니다

예를 들어 첫 번째 것은 HTTPS 보안의 강도를 정의합니다

하지만 그냥 저장하겠습니다

이제 로드 밸런서와 호환되는 리스너가 생겼습니다

포트 443에 있습니다

완벽합니다

나중에 보안 그룹과 관련하여 몇 가지를 변경해야 할 수도 있지만, 지금은 괜찮습니다

다음을 클릭합시다

여기서는 아무것도 필요하지 않습니다

그래서 그냥 다음을 클릭하겠습니다

모든 것을 검토하고 제출합시다

이제 Beanstalk이 로드 밸런서와 오토 스케일링 그룹

그리고 EC2 인스턴스를 사용하여 환경을 시작할 것입니다

로드 밸런서에 443 리스너를 생성했습니다

그리고 TLS 또는 SSL 인증서를 로드 밸런서에 직접 로드했습니다

환경이 시작될 때까지 기다렸다가 다시 돌아오겠습니다

좋아요, 환경이 성공적으로 시작되었습니다

그러면 이 도메인을 클릭할 수 있습니다

지금 보시다시피 Beanstalk URL을 사용하여 접속하고 있습니다

하지만 우리는 acm-demo URL을 사용하여 접근하고 싶습니다

그래서 먼저 Route53에서 CNAME을 생성해야 합니다

이를 위해 Route53으로 가서 호스팅 영역 아래에서 R1을 살펴보겠습니다

stefantheteacher.com이고, 레코드를 생성할 건데 acm-demo가 될 것입니다

이것은 CNAME 유형입니다

한 도메인을 다른 도메인으로 연결하기 때문이며, 값을 붙여넣되 HTTP는 제외합니다

그래서 이제 누군가 acm-demo.stefantheteacher.com으로 가면

실제로는 이 URL로 이동하게 됩니다

레코드 생성을 누르면 생성되었습니다

이제 테스트해 봅시다

여기에 붙여넣겠습니다

그러면 URL로 가볼까요?

도메인 이름이 전파될 때까지 조금 기다려야 해요

이제 새로고침되어, URL을 사용해서 제 페이지에 접근할 수 있게 되었습니다

이건 그냥 DNS 전파 문제였네요

이제 괜찮습니다

이 URL로 잘 되고 있습니다

그런데 보시다시피 작은 자물쇠가 없습니다

그러면 https를 추가해서 https로 접근할 수 있는지 확인해 봅시다

네, 가능합니다

보시다시피 이제 자물쇠 아이콘이 연결이 안전하다고 표시하고 있습니다

Amazon에서 검증했습니다

파이어폭스나 크롬 또는 원하는 브라우저를 사용하여

인증서에 대한 더 많은 정보를 얻을 수 있습니다

인증서를 볼 수 있습니다

여기에는 ACM에서 발급한 인증서에 대한 많은 정보가 나와 있습니다

실제로 보안 그룹을 전혀 변경할 필요가 없어서 좋았습니다

그렇다면 AWS 측에서도 모든 것이 올바르게 로드되었는지 어떻게 확인할 수 있을까요?

모든 것이 잘 작동하고 있으니까요

예를 들어, EC2 콘솔로 들어가 봅시다

이제 로드 밸런서를 살펴보겠습니다

여기에서 로드 밸런서를 찾을 수 있습니다

그리고 리스너와 규칙을 확인합니다

여기에서 https 443에 대한 규칙이 있음을 볼 수 있습니다

클릭해 보면 ACM에서 직접 로드된

기본 SSL TLS 인증서가 있음을 확인할 수 있습니다

원하신다면 기본값을 변경할 수도 있습니다

또는 이 UI에서 직접 인증서를 더 추가할 수도 있습니다

하지만 이대로도 좋습니다

ACM과 Elastic Beanstalk, 그리고 로드 밸런서 간의 통합을 확인했습니다

이제 모든 것을 정리하고 지속적인 비용이 발생하지 않도록 해야 합니다

이 환경으로 가시거나, 더 쉽게는 애플리케이션으로 가세요

그런 다음 액션(Actions)을 선택하고 애플리케이션 삭제(Delete application)를 클릭하세요

애플리케이션의 전체 이름인 DemoTLSCertificate-env를 입력하면 완료됩니다

이번 강의는 여기까지고 강의가 마음에 드셨길 바라며

앞서 AWS Certificate Manager에서 퍼블릭 인증서를 발급하는 걸 봤었죠, 사실 우리는 사설(Private)

인증서도 발급할 수 있습니다

사설 인증서를 발급하려면 AWS Private CA를 구성해야 합니다

이는 관리형 서비스로, 이를 사용해 루트 인증 기관 또는 하위 인증 기관을 생성할 수 있습니다

여기서 하위 인증 기관(CA)이란 루트에 종속된 인증 기관을 뜻합니다

우리는 여기에서 엔드 엔티티 x.509 인증서를 발급해 배포할 수 있습니다, 즉 애플리케이션에서

사용할 수 있는 인증서라는 뜻이죠

이 인증서는 새로운 인증서를 만드는 데 사용할 수 없습니다

이 인증서는 AWS 조직 내에 있는 애플리케이션에서만 신뢰할 수 있습니다, 사설 인증 기관을

신뢰하도록 설정되어 있는 경우에 한해서요

이 인증서는 공용 인터넷에 배포할 수 없습니다

사설 CA에서 발급한 인증서기 때문에 동작하지 않을 겁니다, 추가로

여러분이 사용 중인 AWS 서비스에서 애플리케이션 로드 밸런서와 같은 ACM을 지원하고 있다면

거기에 여기서 발급한 사설 인증서를 로드해 사용할 수 있습니다

이런 식으로 CloudFront, API Gateway, 로드 밸런서, 쿠버네티스 서비스 등에

로드할 수 있어요

필요하다면 이 인증서를 사용자에게 발급할 수도 있고, 특정 컴퓨터, 특정

API 또는 엔드포인트 장치나 IoT 기기에 발급할 수 있습니다

사설 CA가 주로 사용되는 곳은 내부적으로 암호화된 TLS 통신을 사용하는 경우나

코드에 암호화 서명을 적용하는 경우, 사용자 또는 컴퓨터 및 API 엔드포인트 장치, IoT 기기 인증을 위해

인증서를 제공해야 하는 경우, 기업용 공개 키 기반 구조를 구성하는 경우입니다

여기까지입니다

도움이 됐길 바랍니다, 그럼 다음 시간에 뵙겠습니다

이번엔 Macie에 대해 알아볼까요?

Macie는 완전 관리형 데이터 보안 및 데이터 프라이버시 서비스예요

Macie는 머신러닝과 패턴 매칭을 이용해서

AWS에 있는 여러분의 민감한 정보를 발견하고 보호하죠

더 구체적으로 Macie는 PII라고 부르는 개인식별정보 등

민감한 데이터에 대해 경보를 제공해요

아주 간단하게 보자면, 여러분의 PII가 S3 버킷에 있으면

Macie는 그걸 분석하고요, Macie는 어떤 데이터를 PII로 분류할 수 있는지 확인할 거예요

그리고 EventBridge를 통해 발견 결과를 알려 주죠

그러면 여러분은 그걸 SNS Topic이나 Lambda 함수 등에 통합할 수 있어요

이 경우에는 Macie를 사용해서

S3 버킷에 있는 민감한 정보를 확인할 거예요, 그것만 하게 되죠

클릭 한 번으로 활성화할 수 있고요, 여러분은 그냥 S3 버킷만 지정하면

그걸로 끝이죠

그럼 이번 강의는 여기까지 할게요, 순식간에 마쳤는데 Macie에 대해서는 이 정도로 충분해요

유익했길 바라고요, 저는 다음 강의에서 뵐게요

이제 AWS AppConfig에 대해 알아봅시다

애플리케이션을 구성하기 위해 우리가 흔히 사용하는 방법 중 한 가지는

앱 구성 정보를 앱과 함께 배포하는 겁니다

때로는 환경 변수를 사용해 구성 정보를 모듈화하기도 하죠

그런데 구성 데이터를 앱 외부에 두고 싶다면 어떻게 해야 할까요?

이럴 때 AppConfig를 사용합니다, AppConfig를 사용하면 앱을 구성 및 검증하고 구성 데이터를

동적으로 배포할 수 있습니다, 구성 데이터를 변경해도 애플리케이션은 이와 상관없이 애플리케이션에

새로운 코드가 배포되었을 때 변경된 구성을 반영합니다

애플리케이션을 다시 실행하지 않아도

모든 게 동적으로 반영됩니다

이 서비스의 장점 중 한 가지는 '기능 플래그'를 사용할 수 있다는 겁니다

기능 플래그가 뭘까요?

애플리케이션에 새로운 기능을 추가했는데 지금 당장은 비활성화하고 싶다고 해봅시다

그럼 그냥 애플리케이션을 배포하고

AppConfig에서 해당 기능만 꺼두면 됩니다, 그런 다음 애플리케이션 배포가 완료되고

테스트 준비가 끝나면 AppConfig에서 기능 플래그 설정을 바꿔주고요, 그럼 애플리케이션이

바뀐 플래그 값을 반영해 자동으로 해당 기능을 활성화시킬 수 있죠

뿐만 아니라

거의 모든 구성 정보를 동적으로 변경할 수 있습니다, 따라서 애플리케이션 성능을 미세하게

조정하는 것도 가능하고

심지어 차단할 IP 목록이나 허용할 IP 목록을 AppConfig에서 변경해 실시간으로 반영하는 것도

가능합니다, 애플리케이션 코드를 변경하지 않고도요

E2 인스턴스, Lambda, ECS, EKS 등에서 구동되는 앱에 유용하게 사용할 수 있어요

그리고 구성 정보를 변경했을 때, 예를 들어 기능 플래그 설정을 변경했다고 해볼게요

기능을 모든 인스턴스에서 동시에 공개하고 싶지 않다고 해봅시다

변경 사항을 점진적으로 배포하면서 문제가 발생하면 되돌릴 수 있도록요

AppConfig에서는 이런 것도 가능합니다

구성 소스로 쓸 수 있는 곳은 다양합니다

파라미터 저장소가 될 수도 있고, SSM 문서나 S3 버킷 등의 장소를 지정할 수 있죠

EC2 인스턴스에서는 애플리케이션이 실행되고 있겠죠, 애플리케이션은 주기적으로

구성 변경 사항을 폴링해 이를 가져갈 겁니다

우리가 구성 정보를 변경하면 CloudWatch가 자동으로 이를 모니터링해

잘못된 점이 없는지 확인합니다

문제가 있다면 경보를 발생시킬 거고 이 경보가 구성 변경을 철회할 겁니다, 여기에 추가로

잘못된 구성 정보가 배포되지 않도록 하기 위해

검증하는 방법이 있습니다, JSON 스키마를 사용하면 구성 데이터가 특정 형식으로 작성되었는지

등을 확인할 수 있습니다, 또는 Lambda 함수를 쓸 수도 있죠

여기서는 코드를 사용합니다, 좀 더 복잡한 방식으로 매개변수를 확인하기 위해

사용하죠

여기까지예요

이게 AppConfig입니다

도움이 됐길 바랍니다, 그럼 다음 시간에 뵙겠습니다



그럼 이제 CloudWatch Evidently에 대해 알아보죠

이건 여러분이 앱의 새로운 피처를 테스트할 수 있게 해주는 CloudWacth의 기능이고요

사용자의 일부분에게만 그 피처를 제공하는 기능이에요

그럼 왜 이걸 사용할까요?

예, 우선 여러분이 새로운 피처를 릴리스했을 때 위험을 줄이려는 경우가 있어요

여러분은 예를 들어 그 피처를 사용자의 5%에게만 제공해서 그렇게 할 수 있죠

그렇게 해서 의외의 결과를 파악할 수 있게 되죠

또 여러분은 실험 데이터를 수집해서 통계를 통해 그걸 분석하고

새로운 피처의 성능을 모니터링해서 그 피처를 테스트하려 할 수도 있어요

그리고 CloudWatch Evidently로 할 수 있는 두 가지 활용 사례가 있는데요

첫째는 Launches예요, 일반적으로는 피처 플래그라고 부르고요

이 경우엔 일부 사용자에게 피처를 활성화하거나 비활성화할 수 있어요

예를 들면 일부 사용자가 여러분의 앱에 코멘트를 달도록 할 수도 있죠

하지만 모든 사람이 그 피처를 갖도록 하고 싶지 않아요, 그리고 그걸 모든 사람에게 제공하기 전에 여러분은

그 피처를 테스트하려고 해요, 그럼 그게 피처 플래그가 될 거고요

일부 사용자는 그 좋아요 버튼을 보고 일부 사용자는 보지 못할 거예요

다른 하나는 Experiments인데요

이건 A/B 테스팅이고요, 같은 피처를 가진 다수의 버전들을 비교하기 위한 것이죠

그럼 예를 들어 좋아요 버튼이 왼쪽에 있으면 어떤지

오른쪽에 있으면 어떤지, 어떤 게 가장 효과적일지 비교하는 것이에요

그럼 요약하자면, CloudWatch Evidently에서 개발자로서 우리는 프로젝트와 피처를 만들고

실험을 하죠

우린 코드 스니펫을 되돌려 받아서 그걸 앱에 내장하고요

그게 앱에 내장되면 우리 사용자들은 정상적으로 앱에 액세스하게 되죠

하지만 우리는 CloudWatch Evidently로 돌아가서 예를 들어 피처 플래그 같은

새로운 피처에 액세스할 수 있는 사용자의 비율을 지정할 거예요

또는 A/B 테스팅의 경우엔 버전 a가 아닌

버전 b를 봐야 하는 사용자의 비율을 지정해요

그런 다음에 사용자가 우리 앱에 액세스하면

그들은 우리가 CloudWatch Evidently에서 설정한 것을 보게 되죠

이런 식으로 해서 여러분은 새로운 피처의 런치를 테스트하거나 A/B 테스팅 실험을 할 수 있어요

그럼 Launches와 Experiments라는 용어를 기억해 두시고요, 그리고 마지막으로 기억해야 할 용어는 Overrides예요

가령 우리 조직 안에 베타 테스터가 있다고 해보죠

그리고 우린 우리가 릴리스하는 새 피처에 베타 테스터만 액세스할 수 있도록 하고 싶어요

왜냐면 베타 테스터가 그걸 테스트하길 원하기 때문이죠

그런 경우에 우린 그게 Evidently를 통하도록 할 수 없어요

왜냐면 그 피처가 베타 테스터에게 활성화될지의 여부가

무작위로 결정되기 때문이죠

그 대신에 우린 Overrides를 생성해요

이 Overrides는 우리가 CloudWatch Evidently에서 직접 정의하는 건데요

그리고 예를 들어, 우리 베타 테스터의 사용자 ID 같은

특정한 사용자 ID를 가진 사용자에게만

새로운 피처 혹은 버전 b를 표시하라고 하는 것이죠

그러면 우리의 베타 테스터가 앱에 액세스할 때마다

Overrides가 작동하게 될 거예요

그리고 베타 테스터는 우리가 오버라이드하도록 설정한 걸 모두 확인할 수 있겠죠

이 부분은 시험에 나올 수 있어요

마지막으로, 실험 데이터를 저장하기 위해 CloudWatch Logs나 Amazon S3를 사용할 수 있어요

이번 강의는 여기까지예요, 마음에 드셨길 바라고요, 저는 다음 강의에서 뵐게요

